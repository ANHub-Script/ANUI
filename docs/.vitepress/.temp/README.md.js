import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ANUI Documentation","description":"","frontmatter":{},"headers":[],"relativePath":"README.md","filePath":"README.md","lastUpdated":1787760131000}');
const _sfc_main = { name: "README.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="anui-documentation" tabindex="-1">ANUI Documentation <a class="header-anchor" href="#anui-documentation" aria-label="Permalink to &quot;ANUI Documentation&quot;">​</a></h1><p>The official documentation site for <strong>ANUI</strong> (Advanced Roblox UI Library), built with <a href="https://vitepress.dev" target="_blank" rel="noreferrer">VitePress</a>. Fully translated into five languages: <strong>English</strong> (default), <strong>Bahasa Indonesia</strong> (<code>/id/</code>), <strong>日本語</strong> (<code>/ja/</code>), <strong>Русский</strong> (<code>/ru/</code>) and <strong>简体中文</strong> (<code>/zh/</code>) — 36 pages per locale, 180 in total.</p><ul><li><strong>Live site:</strong> <a href="https://ANHub-Script.github.io/ANUI/" target="_blank" rel="noreferrer">https://ANHub-Script.github.io/ANUI/</a></li><li><strong>Repo:</strong> <a href="https://github.com/ANHub-Script/ANUI" target="_blank" rel="noreferrer">https://github.com/ANHub-Script/ANUI</a> (docs live in this <code>docs/</code> folder)</li><li><strong>Discord:</strong> <a href="https://discord.gg/qN47S3mKZA" target="_blank" rel="noreferrer">https://discord.gg/qN47S3mKZA</a> · <strong>YouTube:</strong> <a href="https://www.youtube.com/@ANHubRoblox" target="_blank" rel="noreferrer">https://www.youtube.com/@ANHubRoblox</a></li></ul><hr><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><ul><li><strong>Node.js 18 or newer</strong> (VitePress 1.x requires Node 18+). Check with <code>node -v</code>.</li><li>npm (ships with Node).</li></ul><h2 id="run-it-locally" tabindex="-1">Run it locally <a class="header-anchor" href="#run-it-locally" aria-label="Permalink to &quot;Run it locally&quot;">​</a></h2><p>From <strong>this <code>docs/</code> folder</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">          # first time only — installs VitePress</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docs:dev</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # start the dev server with hot reload</span></span></code></pre></div><p>Then open the URL it prints — <strong><a href="http://localhost:5173/ANUI/" target="_blank" rel="noreferrer">http://localhost:5173/ANUI/</a></strong> (the <code>/ANUI/</code> suffix is expected; see <a href="#base-path--repo-name">Base path</a> below).</p><h2 id="build-preview-a-production-bundle" tabindex="-1">Build &amp; preview a production bundle <a class="header-anchor" href="#build-preview-a-production-bundle" aria-label="Permalink to &quot;Build &amp; preview a production bundle&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docs:build</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # output → .vitepress/dist</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docs:preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # serve the built site locally to sanity-check</span></span></code></pre></div><hr><h2 id="project-structure" tabindex="-1">Project structure <a class="header-anchor" href="#project-structure" aria-label="Permalink to &quot;Project structure&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>docs/</span></span>
<span class="line"><span>├─ .vitepress/</span></span>
<span class="line"><span>│  ├─ config.mts          # site config: nav, sidebar, i18n, theme, search</span></span>
<span class="line"><span>│  └─ theme/</span></span>
<span class="line"><span>│     ├─ index.ts         # extends the default theme</span></span>
<span class="line"><span>│     └─ custom.css       # ANUI brand colors (cyan → magenta)</span></span>
<span class="line"><span>├─ public/</span></span>
<span class="line"><span>│  └─ logo.svg            # site logo / favicon</span></span>
<span class="line"><span>├─ index.md               # English home (hero) page</span></span>
<span class="line"><span>├─ guide/                 # Getting Started + Guide (English)</span></span>
<span class="line"><span>├─ elements/              # Element API reference — 15 elements + overview</span></span>
<span class="line"><span>├─ features/              # Feature guides (config, key system, themes, …)</span></span>
<span class="line"><span>├─ examples/              # Copy-paste recipes</span></span>
<span class="line"><span>├─ api/                   # API cheatsheet</span></span>
<span class="line"><span>├─ id/                    # 🇮🇩 Bahasa Indonesia mirror of everything above</span></span>
<span class="line"><span>├─ ja/                    # 🇯🇵 日本語 mirror</span></span>
<span class="line"><span>├─ ru/                    # 🇷🇺 Русский mirror</span></span>
<span class="line"><span>├─ zh/                    # 🇨🇳 简体中文 mirror</span></span>
<span class="line"><span>├─ package.json</span></span>
<span class="line"><span>└─ README.md              # you are here</span></span></code></pre></div><p>Every English page under <code>guide/</code>, <code>elements/</code>, <code>features/</code>, <code>examples/</code>, and <code>api/</code> has a matching translation under <code>id/…</code>, <code>ja/…</code>, <code>ru/…</code>, and <code>zh/…</code>.</p><hr><h2 id="editing-content" tabindex="-1">Editing content <a class="header-anchor" href="#editing-content" aria-label="Permalink to &quot;Editing content&quot;">​</a></h2><p>Pages are plain Markdown with VitePress extensions (containers like <code>::: tip</code>, <code>::: warning</code>, code groups, etc.). Edit the <code>.md</code> file and the dev server reloads.</p><p><strong>To add a new page:</strong></p><ol><li>Create the English page, e.g. <code>elements/my-element.md</code>.</li><li>Create its translations: <code>id/elements/my-element.md</code>, <code>ja/elements/my-element.md</code>, <code>ru/elements/my-element.md</code>, <code>zh/elements/my-element.md</code>.</li><li>Register it once in <code>.vitepress/config.mts</code> — the <code>sidebar(prefix, lang)</code> helper builds the tree for every locale from a single definition, so adding one entry there covers all five languages. If the entry needs a translated label, add a key to the <code>UI</code> dictionary at the top of the file with a value for each of <code>en</code>, <code>id</code>, <code>ja</code>, <code>ru</code>, <code>zh</code>.</li></ol><p><strong>Internal links are locale-relative and omit the base:</strong> English pages link like <code>/elements/toggle</code>; the translations link like <code>/id/elements/toggle</code>, <code>/ja/elements/toggle</code>, <code>/ru/elements/toggle</code>, <code>/zh/elements/toggle</code>. VitePress prepends the base automatically — never hard-code <code>/ANUI/</code> in a link.</p><p><strong>Navigation &amp; UI strings</strong> (navbar, sidebar group titles, search labels, footer, prev/next) live in a single <code>UI</code> dictionary in <code>config.mts</code>, keyed by string then language, so all five locales stay in sync. Element names (<code>Button</code>, <code>Toggle</code>, …) are API identifiers and stay in English in every locale.</p><p><strong>To add a language:</strong> add its code to the <code>Lang</code> type, add a translation to every <code>UI</code> entry, add a <code>searchTr(...)</code> line, and register it in <code>locales</code> at the bottom of the file.</p><hr><h2 id="deployment-github-pages" tabindex="-1">Deployment (GitHub Pages) <a class="header-anchor" href="#deployment-github-pages" aria-label="Permalink to &quot;Deployment (GitHub Pages)&quot;">​</a></h2><p>Docs are deployed straight from this repo (<code>ANHub-Script/ANUI</code>) via the GitHub Actions workflow at <a href="../.github/workflows/deploy-docs.yml"><code>../.github/workflows/deploy-docs.yml</code></a>. It builds this <code>docs/</code> folder and publishes it to GitHub Pages on every push to <code>main</code>.</p><p><strong>One-time setup:</strong></p><ol><li>Make sure the <code>docs/</code> folder is committed (it must <strong>not</strong> be git-ignored).</li><li>Push to GitHub.</li><li>In the repo: <strong>Settings → Pages → Build and deployment → Source → GitHub Actions</strong>.</li><li>Push to <code>main</code> (or run the workflow manually via <strong>Actions → Deploy Docs → Run workflow</strong>). The site goes live at <strong><a href="https://ANHub-Script.github.io/ANUI/" target="_blank" rel="noreferrer">https://ANHub-Script.github.io/ANUI/</a></strong>.</li></ol><h3 id="base-path-repo-name" tabindex="-1">Base path &amp; repo name <a class="header-anchor" href="#base-path-repo-name" aria-label="Permalink to &quot;Base path &amp; repo name&quot;">​</a></h3><p>VitePress needs to know the sub-path it&#39;s served from. In <code>.vitepress/config.mts</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">base</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;/ANUI/&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span></code></pre></div><p><strong>This must equal <code>/&lt;your-repo-name&gt;/</code>.</strong> It&#39;s currently <code>/ANUI/</code>, which serves the site at <code>https://ANHub-Script.github.io/ANUI/</code>.</p><ul><li>Repo stays named <strong><code>ANUI</code></strong> → keep <code>base: &#39;/ANUI/&#39;</code>. ✅</li><li>If you rename the repo (e.g. to <code>ANUI-Docs</code>) → change <code>base</code> to match (<code>/ANUI-Docs/</code>).</li><li>Using a <strong>custom domain</strong> or a <code>&lt;user&gt;.github.io</code> <strong>user page</strong> → set <code>base: &#39;/&#39;</code>.</li></ul><hr><h2 id="credits" tabindex="-1">Credits <a class="header-anchor" href="#credits" aria-label="Permalink to &quot;Credits&quot;">​</a></h2><p>ANUI is based on <a href="https://github.com/Footagesus/WindUI" target="_blank" rel="noreferrer">WindUI</a> by Footagesus. Documentation content © 2024–present ANHub-Script. Released under the MIT License.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("README.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const README = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  README as default
};
